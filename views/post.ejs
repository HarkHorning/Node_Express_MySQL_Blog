<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contemporary Wizard</title>
</head>
<body>

    <svg class="svg-object">
        <filter id="filter-effect">
            <feTurbulence type="fractalNoise" baseFrequency="1"/>
        </filter>
    </svg>
    <div class="filter-object"></div>


    <section class="main-section">
        <a href="/" class="return-button">Return to Home Page</a>
        <div href="" class="blog-card">
            <h2 class="blog-title"><%= data.title %></h2>
            <p class="blog-content">Hello stranger,</p>
            <p class="blog-content one"><%= data.content %></p>
            <p class="blog-content two"><%= data.contentTwo %></p>
            <p class="blog-content three"><%= data.contentThree %></p>
            <p class="blog-content four"><%= data.contentFour %></p>
            <p class="blog-content">Take heart and be hopeful, <br>Contemporary Wiz</p>
        </div>
    </section>

    <style>
        * {
            font-family: 'Inter';font-size: 22px;
        }
        body {
            display: flex;
            justify-content: center;
            background-color: rgba(0, 29, 156, 0.815);   
            min-height: 100svh;
            position: relative;
            padding-top: 5rem;
        }
        .svg-object {
            position: absolute;
        }

        .filter-object {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            filter: url(#filter-effect);
            z-index: -2;
        }
        
        .blog-title {
            font-size: 2rem;
            font-weight: 800;
            margin-bottom: 2rem;
        }
        .blog-content {
            font-size: 1.25rem;
            font-weight: 500;
            line-height: 2rem;
        }
        .main-section {
            max-width: 1000px;
            display: flex;
            flex-direction: column;
            /* justify-content: center; */
            /* align-items: center; */
            padding: 1.5rem;
            height: 100%;
        }
        .return-button {
            position: absolute;
            top: 2.5rem;
            left: 2.5rem;
            border: none;
            background-color: rgba(0, 0, 0, 0);
            font-size: 1rem;
            font-weight: 900;
            color: rgba(255, 255, 255, 0.39);
            text-decoration: none;
        }
    </style>
</body>
</html>